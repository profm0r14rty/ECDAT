# ECDAT Project Progress

## Batch 1 Summary

ECDAT's scanner core (Batch 1) is complete and tested end-to-end. It now
discovers cryptographic artefacts in source code across Python, JavaScript,
Java, Go, and C/C++ via regex signatures, assesses each artefact's post-quantum
risk using Mosca's inequality (with key-size- and path-based heuristics),
recommends NIST post-quantum replacements (ML-KEM, ML-DSA, SLH-DSA, AES-256),
and exports a real CycloneDX 1.6 CBOM report plus a frontend-friendly summary.
A CLI entrypoint (`python -m ecdat_core.cli scan <path>`) ties the pipeline
together, and an end-to-end test scans a mixed-language `demo_repo` fixture to
prove critical and quantum-safe outcomes flow through to a structurally valid
CBOM. The full suite stands at 92 passing tests (2 network-gated tests
deselected by default).

| Phase | Description | Status | Commit | Notes |
|-------|-------------|--------|--------|-------|
| 0: Repo scaffold | Initialize git repo, create project structure, AGENTS.md, progress.md | Done | 68505bd | 2026-09-08 |
| 1: CBOM-aligned Pydantic data models | Detection, RiskAssessment, Recommendation, ScanResult with validators | Done | 83fc732 | 2026-09-08 |
| 2: Cryptographic signature knowledge base | signatures.json + signature_loader.py with Pydantic validation + pytest | Done | 0fd0e9b | 2026-09-08 |
| 3: Regex-based detection engine | detector.py (scan_file_content + detect_language) + detector tests (47 passing) | Done | 09b8f0e | 2026-09-08 |
| 4: Repo and manifest ingestion | ingestion.py (ingest_local_directory, ingest_git_url, ingest_manifest_dependencies) + ingestion tests (62 passing) | Done | 7d721a2 | 2026-09-08 |
| 5: Mosca's algorithm risk engine | risk_engine.py (assess_risk with X/Y/Z heuristics, urgency ratio, risk levels) + 5 risk-engine tests (67 passing) | Done | de96e53 | 2026-09-08 |
| 6: PQC recommendation engine | recommender.py (recommend pass-through of pqc_recommendation + "no migration needed" for existing PQC) + 3 recommender tests (70 passing) | Done | 03204b4 | 2026-09-08 |
| 7: CycloneDX 1.6 CBOM export | cbom_export.py (export_cbom + export_summary) + 18 cbom-export tests (88 passing) | Done | 6871169 | 2026-09-08 |
| 9: Backend scaffold, SQLAlchemy models, Alembic migration | FastAPI app, SQLAlchemy 2.0 ORM models, Alembic migration with ScanRun, DetectionRow, RiskAssessmentRow, RecommendationRow models; health endpoint test | Done | 8479a90 | Created backend/ project structure, models_orm.py, db.py, main.py, Alembic migration, test_health.py |
| 10: Persistence layer mapping ecdat_core results to Postgres | Repository layer translating between ecdat_core Pydantic models and SQLAlchemy ORM rows; supports create/mark-scan-status, persist, get, list, update operations with SQLite test coverage | Done | — | 13/13 tests passing |
| 11: Redis-backed job status and background scan execution | jobs.py: thin redis-py wrapper (REDIS_URL env, default redis://localhost:6379/0, 1s socket timeouts so a down Redis fails fast in ~0.01s with a clear RuntimeError instead of hanging), set_job_status/get_job_status on `scan:{scan_id}:status` keys with 24h TTL (`set(..., ex=86400)`), and run_scan_job(scan_id, target, source_type) for FastAPI BackgroundTasks — fail-fast `running` in Redis, then ecdat_core.cli.run_scan(target, is_git_url=(source_type=="git_url")) (already library-callable, no refactor needed), persist_scan_result + mark_scan_done + best-effort `done`; on any exception mark_scan_failed + best-effort `failed`; never re-raises. fakeredis added as a test-only dev dependency; 8 new tests use fakeredis + throwaway SQLite to lock queued->running->done, queued->running->failed (forced exception), 24h TTL, key pattern, and fail-fast-when-Redis-down. Full suite: 124 passing (2 network deselected) | Done | — | 8/8 jobs tests passing |
| Fix Phase 1 | Split `classically_broken` from `quantum_vulnerable` (required field on every signature entry), replace AES-128 entry with generic AES entry (`min_quantum_safe_key_bits=192`, dynamic per-detection quantum_vulnerable in detector.py), add verified PyCryptodome coverage (RSA/DSA/ECC/AES/DES/3DES/ARC4/MD5/SHA-1/DSS), hashlib.sha1 verified present; 97 tests passing | Done | 3a295c7 | 2026-09-09 |
| Fix Phase 2 | Propagate `classically_broken` onto `Detection` (additive `bool = False` default, no breaking change to existing constructions), set it from `signature_entry.classically_broken` in `scan_file_content`; dynamic per-detection AES `quantum_vulnerable` (min_quantum_safe_key_bits=192 threshold) confirmed already implemented in `_resolve_quantum_vulnerable`; new detector tests lock AES-128 → vulnerable, AES-256 → detection produced + quantum-safe, bare AES → conservative fallback with key_size None, MD5 → classically_broken, PyCryptodome `from Crypto.PublicKey import RSA` import (previously non-matching per Phase 3); verified no AES over-matching of words containing "aes"; 99 tests passing | Done | 65c55a6 | 2026-09-09 |
| Fix Phase 3 | `assess_risk()` branch order per AGENTS.md: new first branch for `classically_broken` → always `risk_level="critical"` + `mosca_violation=True` with the naive `(X+Y)/Z` urgency ratio floored at 1.0 (X/Y/Z still computed via normal heuristics for transparency; Pydantic consistency validator holds since floored ratio is exactly >= 1.0); quantum-safe short-circuit now only reachable when `classically_broken` is False, so MD5/DES/3DES/RC4/SHA-1 are never mislabelled "quantum-safe" (on the then-current pre-dedup detection set: demo_repo MD5×5 + DES now critical vs previously quantum-safe; summary went critical 1→7, quantum-safe 10 unchanged — counts later corrected to 6/7 by Fix Phase 5's same-line dedup). Note: no `signatures.json` entry behavior changed — the misclassification was purely a risk-engine branch-order bug. Two new risk-engine tests: classically_broken MD5-like → critical/mosca_violation/urgency≥1.0 with floor demonstrated (naive 0.275 floored to 1.0), and genuinely quantum-safe AES-256 still short-circuits to "quantum-safe". Full suite: 101 passing (2 network deselect), up from 99 | Done | b10a424 | 2026-09-09 |
| Fix Phase 4 | CBOM export property `ecdat:classicallyBroken` added to `export_cbom()` populated from each Detection's `classically_broken` field; CLI scan re-validated: RSA-1024 critical (Mosca), MD5 and DES critical (classically broken), AES-256 quantum-safe; end-to-end test updated to assert at least 2 distinct critical detections with differing `classically_broken` values, preventing silent regression | Done | bad4920c039923b8322b76313fac44943df44add | 2026-09-09 |
| 12: Scan creation, listing, and status/summary endpoints | FastAPI scan API: `POST /api/scans` (202, creates queued ScanRun via repository, schedules `run_scan_job` as BackgroundTask), `GET /api/scans` (most-recent-first list), `GET /api/scans/{id}` (full detail; 404 for unknown; when status=done includes an `export_summary()`-shaped summary computed on the fly from persisted rows via new `backend/app/summary.py`, which reconstructs an in-memory ScanResult and delegates to `ecdat_core.cbom_export` rather than duplicating aggregation logic); routes live in `backend/app/routers/scans.py`, main.py stays thin. 4 new API tests use TestClient + throwaway SQLite (dependency override + patched job session) + fakeredis: POST demo_repo local_path scan, poll GET detail until done (hard 30s timeout, fails clearly on timeout/failed), assert summary matches Fix-Batch corrected fixture numbers (files 4, detections 13, critical 6, quantum-safe 7, 1 quantum-vulnerable = 7.69%, top-5 = 4×MD5 + DES @ urgency 5.5 — as corrected by Fix Phase 5 same-line dedup); list test; 422 on bad source_type; 404 on unknown id. Full suite: 128 passing (2 network deselected), up from 124 | Done | — | 2026-09-09 |
| 13: Artefact listing, CBOM/report endpoints, per-artefact risk overrides | FastAPI scan API additions: `GET /api/scans/{id}/artefacts` (paginated/filterable list flattening each detection + risk assessment + recommendation into one artefact object, with total/total_pages/page/page_size metadata; filters join persisted risk assessments; `page`≥1, `page_size` 1–500), `GET /api/scans/{id}/cbom` (CycloneDX 1.6 BOM as JSON download with `Content-Disposition: attachment; filename=cbom.json`), `GET /api/scans/{id}/report` (`export_summary()`-shaped, byte-identical to the detail endpoint's `summary` field), `PATCH /api/scans/{id}/artefacts/{detection_id}` (body `shelf_life_years`/`migration_time_years` nullable floats; re-runs `ecdat_core.risk_engine.assess_risk` against the persisted Detection + its SignatureEntry and persists via `repository.update_risk_assessment`; `None` = reset to default heuristics). All artefact-level endpoints: 404 unknown scan, 409 `"Scan not complete"` when status != done (queued/running), PATCH additionally 404 when detection_id doesn't belong to scan_id. **Decision (CBOM vs cache): reconstruct the ScanResult from persisted rows on every request** — a CBOM dict cached at persist time would go stale the moment a PATCH override lands (cached pre-override data would contradict every other read path). Repository: removed a leftover placeholder bug in `get_detections` — the unconditional `classically_broken != True` filter silently excluded ALL classically-broken detections (MD5/DES) whenever any risk_level filter was applied; replaced with a clean 1:1 join (unique FK) onto risk_assessments. New repository helpers `detection_row_to_model`, `get_detection_for_scan`, `get_detection_triples`; `summary.py` extracted shared `build_scan_result_from_rows` (single reconstruction path for detail/report/cbom). Test fixtures (`client`, new `db_session_factory`, `wait_for_scan_done`) moved to conftest.py and shared by both API test modules. 10 new tests lock: pagination counts (13 total / 3 pages of 5 / last page 3 — corrected by Fix Phase 5 dedup from 17/4/2), risk_level+algorithm_family filters (critical 6, quantum-safe 7, MD5 4, combined 4), 422 on page=0/page_size=501, CBOM structure + header + 13 components, report == detail summary, PATCH baseline critical 2.333 → shelf=0 override low 0.111 → GET reflects → null reset restores critical, 422 on negative overrides, 404 for foreign/unknown detection ids, 409 while queued+running then 200 when done, 404 for unknown scans. Full suite: 138 passing (2 network deselected), up from 128 | Done | — | 2026-09-09 |

| 14: Docker Compose wiring for postgres, redis, and the API | docker-compose.yml rewritten: `postgres` (postgres:16-alpine, env from new `.env.example`, named volume `postgres_data`, `pg_isready` healthcheck), `redis` (redis:7-alpine, `redis-cli ping` healthcheck), `api` (build context = repo root + `backend/Dockerfile` — required because the API imports `ecdat_core` from the monorepo root, so `build: ./backend` alone was impossible; documented deviation. `depends_on` postgres+redis with `condition: service_healthy`, DATABASE_URL/REDIS_URL pointing at compose service names, `8000:8000`, `./backend` bind-mount + uvicorn `--reload` for dev hot-reload, entrypoint runs `alembic upgrade head` then execs uvicorn); commented-out `web` placeholder for Batch 3 frontend. New: `backend/Dockerfile` (python:3.12-slim, PYTHONPATH=/app so `import backend.*` resolves to the mounted source — the local env only works because commands run from the repo root), `backend/docker-entrypoint.sh`, root `.env.example`, root `.dockerignore`. README gets a "Running locally" section (docker compose up --build + curl localhost:8000/health). **Fixed 3 latent Phase 9/10 defects the container exposed**: (1) `initial.py` lacked `revision`/`down_revision`, so alembic treated the tree as empty and `upgrade head` no-op'd exit 0 with zero tables — rewrote with enums matching `models_orm` values, plus a module docstring documenting the rewrite; (2) `alembic/env.py` defined the offline/online runners but never dispatched them — added the standard `is_offline_mode()` dispatch block; (3) SQLAlchemy 2.0 persists enum member *names* by default (`quantum_safe`, `related_crypto_material`), which Postgres native enums reject (SQLite had masked it as unconstrained VARCHAR) — added `values_callable` storing member values on all four enum columns in `models_orm.py`. No `signatures.json` behavior changed. Verified with Docker (available in this env): clean-volume `docker compose up --build` → 3/3 healthy, entrypoint migrated fresh Postgres to 4 tables + alembic_version, `GET /health` → 200 `{"status":"ok"}`, live hot-reload confirmed, E2E scan of demo_repo through the containerized API → status done, summary exactly matches fixture (13 detections, critical 6, quantum-safe 7, 1 quantum-vulnerable — corrected by Fix Phase 5 dedup), Postgres row counts match, Redis `scan:{id}:status` = done. Full suite: 138 passing (2 network deselected). Stack torn down afterwards (`docker compose down`, volume kept) | Done | — | 2026-09-09 |
| 15: End-to-end integration test covering the full Batch 2 API surface | New `backend/tests/test_integration_e2e.py`: one test walks the whole Batch 2 API against a throwaway SQLite DB + fakeredis (Phase 11 convention) — `POST /api/scans` (local_path → demo_repo fixture) → 202; poll `GET /api/scans/{id}` until done (30s timeout-guarded via shared `wait_for_scan_done`); `GET /api/scans/{id}/artefacts` asserts the Fix-Batch corrected counts (13 total, 6 critical — after Fix Phase 5 dedup) and that the critical filter spans both `classically_broken=True` (MD5/DES) and `classically_broken=False` (RSA-1024) artefacts; `GET /api/scans/{id}/cbom` parses as JSON with `bomFormat=="CycloneDX"`, `specVersion=="1.6"`, and 13 components matching the artefact count; `PATCH /api/scans/{id}/artefacts/{detection_id}` overrides RSA-1024's `shelf_life_years` 10→0 and the returned `urgency_ratio` collapses from `10.5/4.5` (critical) to `0.5/4.5` (low) exactly per Mosca's formula, then a re-GET confirms the override persisted. Full suite: 139 passing (2 network deselected), up from 138 | Done | — | 2026-09-09 |
| Fix Phase 5 | **Real bug, not just investigation**: `scan_file_content` produced multiple Detections for the same physical line whenever a signature entry's pattern strings co-occurred. Diagnostic over demo_repo found 4 duplicate lines across 2 entries: 3 × AES (`\bAES-\d+\b` and `\bAES\b` both matching "AES-256" in modern/crypto.py:4,24,27) and 1 × MD5 (`hashlib\.md5\(` and `md5\s*\(` both matching hasher.py:10). Fix: `_dedupe_detections()` collapses raw matches per (signature, line) keeping the highest-confidence match, then on a tie the most-specific (longest matched_text) — e.g. `AES-256` over bare `AES`, `hashlib.md5(` over `md5(`; rule documented in the helper docstring. 3 new detector tests lock the collision-collapse behavior including the tie-break. **Corrected ground truth for demo_repo: 17 → 13 detections (4 files unchanged), critical 7 → 6, quantum-safe 10 → 7, quantum-vulnerable 1/13 = 7.69% (was 1/17 = 5.88%), top-5 = 4×MD5 + DES @ urgency 5.5.** Backend Phase 12/13/15 expected counts updated (summary, pagination 13/3/3, filters critical 6 / quantum-safe 7 / MD5 4, CBOM 13 components, report 13/6); `ecdat_core/tests/test_end_to_end.py` and `test_cbom_export.py` verified unaffected (structural/hand-built fixtures). Full suite: 142 passing (2 network deselect), up from 139 | Done | f49e301 | 2026-09-09 |
| 16: Frontend scaffold, API client, routing, CORS | `frontend/` scaffolded from `npm create vite` (react-ts): Vite 8 + React 19 + TS 6, Tailwind CSS v4 via the `@tailwindcss/vite` plugin (no `tailwind.config.js`/`postcss.config.js`; theme lives in `src/index.css` + `vite.config.ts`), shadcn/ui initialized with the current CLI (base: Radix, preset Nova → `baseColor: neutral`; button + card/badge/input/label/table/tabs/skeleton/separator/alert added via `npx shadcn@latest add`; `components.json` tracked), React Router 7 (`createBrowserRouter`: `/` = scan list + new-scan form, `/scans/:id` = scan detail placeholder), axios as the HTTP layer + Recharts installed for the dashboard phases. `src/api/client.ts` is the single typed API boundary (`api.getHealth/createScan/listScans/getScan/getArtefacts/getCbom/getReport/patchArtefact`) — its TS interfaces mirror `backend/app/routers/scans.py` Pydantic models 1:1 (incl. `ScanSummary` shaped from `ecdat_core.cbom_export.export_summary`'s dict). Base URL: `VITE_API_BASE_URL` env var, default = same-origin so Vite's dev proxy (`vite.config.ts` `server.proxy`: `/api` + `/health` → `http://localhost:8000`) keeps dev CORS-free; `backend/app/main.py` gained `CORSMiddleware` (default origins `http://localhost:5173` + `http://127.0.0.1:5173`, overridable via comma-separated `CORS_ORIGINS` env) for direct/containerized-API calls. **Documented deviation (AGENTS.md rule): task said `VITE_API_BASE_URL` default `http://localhost:8000` — but the acceptance criterion requires the health check to reach the backend *through the dev proxy*, so the client's default is same-origin (`''`) with `http://localhost:8000` documented as the value for direct/containerized access; both paths (proxy + CORS) are wired and browser-verified.** Verification: `npm run build` (`tsc -b && vite build`) clean zero TS errors; real-browser (Playwright, headless chromium via CDP) check — `/` renders the routed shell with health badge "Backend ok" (proxied `GET /health` → 200 and `GET /api/scans` → 200 seen in network log), new-scan form (`local_path` → demo_repo) creates a scan and navigates to `/scans/{id}`, detail page renders the persisted record (status `failed` with Redis down — expected fail-fast per `jobs.py`, DB status authoritative); direct-backend CORS preflight returns `access-control-allow-origin: http://localhost:5173`. `AGENTS.md` gained the Frontend Conventions (Batch 3+) section. Full backend suite: 142 passing (2 network deselected) | Done | — | 2026-09-09 |
| 17: Live status polling and in-flight scan state | Live status polling added to scan detail page (1.5s polling interval while queued/running, progress spinner + status label for in-flight scans, error_message displayed on failure, "scan complete, N detections found" placeholder with inert Overview/Artefacts/Recommendations/Export tabs). New Scan form and Scan List already from Phase 16. | Done | ae0725d | 2026-09-09 |
| 18: Scan overview stats and risk distribution chart | Scan detail page's Overview tab fully implemented in `frontend/src/pages/ScanDetailPage.tsx` (was the "full overview lands in a later phase" placeholder): stat cards show total detections, files scanned, quantum-vulnerable count + % share, and a classically-broken count computed client-side by fetching `GET /api/scans/{id}/artefacts` once (`page_size=500`) and counting `classically_broken` -- the `export_summary()` shape (`ecdat_core/cbom_export.py:107`) was verified first and does *not* expose `classically_broken_count`, only `total_detections` / `quantum_vulnerable_count` / `quantum_vulnerable_percentage` / `risk_level_counts` / `algorithm_family_counts` / `top_5_urgency` (confirmed against a live containerized scan of `demo_repo`, scan id `0e9dd431`). Summary comes from exactly one source (`GET /api/scans/{id}` -- never also `/report`); artefacts is the only second fetch and is needed for the classically-broken stat. Risk distribution renders as a Recharts `PieChart` (zero-count levels filtered out so no empty slices) colored from a new shared palette `frontend/src/lib/colors.ts` (`RISK_COLORS`: critical `#ef4444` red / high `#f97316` orange / medium `#eab308` yellow / low `#84cc16` lime / quantum-safe `#22c55e` green; `RISK_LABELS` for display names) -- the same palette also colors the top-5 badges (previous shadcn-variant-only `RISK_LEVEL_VARIANT` const was removed as redundant). Top-5 highest-urgency artefacts list from `summary.top_5_urgency` (field name verified against `export_summary`/`_top_urgency`) ranked 1-5 with algorithm_family, file_path, urgency badge colored by risk level, and `urgency x.x`. Verification: `npm run build` (`tsc -b && vite build`) clean; live browser check against the containerized backend on the completed demo_repo scan renders exactly the Fix Phase 5 corrected numbers -- 13 detections / 4 files / 1 quantum-vulnerable (7.7%) / 5 classically broken / chart Critical 6 + Quantum-safe 7 / top-5 = DES + 4xMD5 all @ urgency 5.5 -- with console errors 0; pie sectors confirmed `#ef4444` + `#22c55e`. Full suite: 142 passing (2 network deselected), unchanged (frontend-only phase) | Done | -- | 2026-09-09 |
| 19: Filterable/paginated artefact table with detail drawer and risk overrides | Artefacts tab built in `frontend/src/pages/ArtefactsTab.tsx`, wired into `ScanDetailPage.tsx` (was the Phase 17 inert placeholder). Driven by `GET /api/scans/{id}/artefacts` via `api.getArtefacts`, never fetch/axios directly. Filters: risk-level select (All/critical/high/medium/low/quantum-safe), data-driven algorithm-family select (options from `summary.algorithm_family_counts`, fallback = families on the current page), page-size select (10/25/50/100), Clear-filters button. Table: Algorithm / File (`truncatePath` keeps tail segments with `…/` prefix, new `frontend/src/lib/paths.ts`) / Line / Risk level / Quantum / Classical / Confidence; `RiskLevelBadge` reuses the Phase 18 palette (`frontend/src/lib/colors.ts`), `QuantumVulnerableBadge` (amber) vs `ClassicallyBrokenBadge` (red) with a muted "—" dash in the opposite column so classically-broken artefacts are never misread as quantum-safe (`frontend/src/pages/artefacts/badges.tsx`); 7-col skeleton/error/empty states; Previous/Next pagination with totals; row click opens `ArtefactDetailDialog` (`frontend/src/pages/artefacts/ArtefactDetailDialog.tsx`): full path + line, matched_text in a mono `<pre>`, language/confidence/method/asset-type meta, Mosca X/Y/Z with urgency + violation badge, recommendation block (recommended_algorithm, FIPS reference, rationale, latency/migration notes), and the override form — shelf_life_years/migration_time_years number inputs, Save overrides → `api.patchArtefact` PATCH, Reset to defaults sends nulls, "Override saved — risk assessment re-run." confirmation. PATCH updates the row in place and re-fetches (via `reloadKey`) only when the new risk falls out of the active filter; dialog remounts via `key={selected?.id ?? 'closed'}` instead of a sync-state effect (oxlint set-state-in-effect clean). shadcn `select` + `dialog` components added via CLI per AGENTS.md. **Live browser verification** (Playwright via CDP against the containerized backend, demo_repo scan `0e9dd431`): filter Critical → exactly 6 rows (RSA-1024 + DES + 4×MD5, the Fix Phase 5 corrected count); RSA shows amber Quantum-vulnerable vs MD5/DES red Classically-broken with "—" in the opposite column; opening RSA-1024 and overriding shelf_life_years 10→0 re-rendered the dialog badge Critical → Low, urgency 2.33 → 0.11, cleared the Mosca-violation badge, and dropped the row out of the Critical filter (5 remaining); re-GET `/artefacts` confirmed persistence (`shelf_life_years` 0.0, `risk_level` low, `mosca_violation` false). `npm run build` (`tsc -b && vite build`) clean, zero TS errors; backend pytest 142 passing (2 network deselected). No `signatures.json` entry behavior changed. | Done | — | 2026-09-09 |
| 20: Mosca's-inequality timeline visualization | Per-artefact visual representation of Mosca's inequality, embedded in the existing Phase 19 detail drawer (`frontend/src/pages/artefacts/ArtefactDetailDialog.tsx`) — chosen over a dedicated "Risk Timeline" tab as the lower-total-work option that already exists, since the drawer already fetches the full artefact (risk assessment with X/Y/Z). New component `frontend/src/pages/artefacts/MoscaTimeline.tsx` renders a horizontal stacked bar: red segment = migration time (X), amber segment = shelf life (Y), with a dashed vertical reference line at threat horizon (Z), plus numeric labels (`X+Y=Nyr` under the bar, `Z=Nyr` above the line) so the actual math is visible to a judge. Visually distinguishes the failure modes per AGENTS.md (never re-conflate the two concepts): **classically-broken** artefacts (MD5/DES) show a full-width red "N/A — already broken by classical attacks" band with the X/Y numbers and no plotted Z line — the quantum threat horizon is *not* the operative constraint for them, so no misleadingly precise bar for a non-operative number; **quantum-safe** artefacts (AES-256) show an all-green "No quantum threat — artefact is quantum-safe" state rather than a zero-length bar against an imaginary Z. Bar scale = max(sum, Z, 1) so both X+Y and Z are always on-screen. The dialog now passes `classicallyBroken` + `quantumVulnerable` (both on `Artefact`, not `ArtefactRiskAssessment` — that interface only carries the three Mosca numbers) into the timeline; the below-the-bar number row now shows `Z = N/A — already broken` for classically-broken artefacts instead of a real Z value. No backend / `ecdat_core` / `signatures.json` changes — purely a frontend visualization of data the risk engine already produces. Verification: `npm run build` (`tsc -b && vite build`) zero TS errors; `npm run lint` shows only pre-existing shadcn fast-refresh warnings (button/badge/tabs), none in the new/changed files | Done | 98317c0 | 2026-09-09 |
| 21: CBOM/report export download + grouped recommendations panel | Two user-facing additions to the scan detail page. **(Part A) Export tab** (`frontend/src/pages/ScanDetailPage.tsx` `ExportTab`): "Download CBOM (CycloneDX 1.6 JSON)" and "Download Report" buttons fetch the file as a browser-downloadable blob (`responseType: 'blob'`) via two new API-client methods `api.downloadCbom`/`api.downloadReport` (never fetch/axios directly per AGENTS.md). Both return `{ blob, filename }`; the filename is read from the response's `Content-Disposition` header via a small `_filenameFromDisposition` helper (CBOM endpoint sends `attachment; filename=cbom.json`, so the real header is used), falling back to a sensible `cbom-<scanId>.json` / `report-<scanId>.json` when the header is absent (the `/report` endpoint serves a plain JSON dict with no `Content-Disposition`). Downloading status + error states included. **(Part B) Recommendations tab** (`RecommendationsTab`): fetches all artefacts once (`page_size=500`), groups by `algorithm_family`, and renders one card per family showing the shared recommendation (recommended_algorithm, fips_reference, rationale) once, plus a count of how many artefacts in the scan are affected and their combined risk profile — a comma-separated `"5 critical, 2 quantum-safe"`-style summary line (word-pluralized, risk levels colored via the Phase 18 `RISK_COLORS` palette, sorted critical→quantum-safe) intended to read like an executive summary a judge/CISO can act on, not a re-listing of the artefact table. Replaces the two Phase 17 inert tab placeholders. Verification: `npm run build` (`tsc -b && vite build`) zero TS errors; `npm run lint` only pre-existing shadcn fast-refresh warnings (button/badge/tabs), none in new/changed files; full pytest suite 142 passed (2 network deselected), no backend code changed; **acceptance verified live** against the containerized backend on the done demo_repo scan (`0e9dd431`): `GET /api/scans/{id}/cbom` returns `Content-Disposition: attachment; filename=cbom.json` and a valid CycloneDX doc (`bomFormat=CycloneDX`, `specVersion=1.6`, 13 `type: cryptographic-asset` components); `GET /api/scans/{id}/report` returns the `export_summary()`-shaped dict with `total_detections/quantum_vulnerable_count/risk_level_counts/algorithm_family_counts/top_5_urgency` keys (no `Content-Disposition`, so the report download uses the `report-<scanId>.json` fallback). | Done | f8e531c | 2026-09-09 |
| 22: Frontend Docker integration + polish + Batch 3 complete | (Part A) `web` service in docker-compose.yml via new `frontend/Dockerfile` (multi-stage: node:22-alpine `npm ci` + `npm run build` → nginx:1.27-alpine serving `/app/dist`), `frontend/nginx.conf` (immutable `/assets/` caching, SPA `try_files` fallback, `/api/` + `/health` reverse-proxied to the `api` service) and `frontend/.dockerignore`; host port `5173:80` (the Vite dev default the AGENTS.md/CORS docs reference), `depends_on: api`. Same-origin runtime choice documented: the bundle keeps its default `VITE_API_BASE_URL` (`''`) and nginx forwards to `api:8000`, so no runtime env substitution or CORS setup is needed in the container flow (mirrors the dev proxy exactly); README service table updated to the four-service stack. (Part B) polish: shared `usePageTitle` hook (`frontend/src/lib/pageTitle.ts`) — route-driven tab titles ("Scan runs · ECDAT — Crypto Discovery Dashboard" / "Scan <id> · …") wired into both pages; class-based top-level `ErrorBoundary` (`frontend/src/components/ErrorBoundary.tsx`, destructive alert + reload fallback) wrapping `<App/>` in `main.tsx`; Overview stat cards skeleton-load the classically-broken count instead of showing a misleading instant 0 (new `loadingArtefacts` state + StatCard `loading` prop); Phase 18 `RISK_COLORS` bumped 500→700-level shades (critical `#b91c1c` / high `#c2410c` / medium `#a16207` / low `#4d7c0f` / quantum-safe `#15803d`) so colored text/pie/badges hit WCAG AA ≥4.5:1 on white (verified: 6.47/5.18/4.92/4.99/5.02 — the 500-levels were 1.92–3.76). (Part C) clean-state verification: `docker compose down -v` → `up --build -d` → 4/4 services healthy with entrypoint auto-migration; full **real-browser** run (headless Chromium via CDP against `http://localhost:5173`) over the containerized stack: health badge through the nginx proxy → scan created from the form (demo_repo `local_path`) → auto-navigate + poll to "Scan complete — 13 detections found" → Artefacts tab (13 rows, correct quantum-vulnerable vs classically-broken badges) → RSA detail-dialog override shelf_life 10→0 (Critical→Low, urgency 2.33→0.11, Mosca violation cleared, "Override saved — risk assessment re-run.", row updated in place) → reset to defaults → Export tab CBOM download (real CycloneDX 1.6: 13 `cryptographic-asset` components with `ecdat:` properties, `content-disposition: attachment; filename=cbom.json`); API-down test: "Backend unreachable" badge + destructive "Could not load scans — Request failed with status code 502" alert, clean recovery on restart; zero console errors on the happy path. No backend / `ecdat_core` code changed (pytest suite still 142 passed, 2 network deselected). | Done | ee6df8d | 2026-09-09 |
| 23: Add Deployment Conventions (Oracle Cloud Free Tier) section to AGENTS.md | Done | 1ec9c2c | |
| 24: Design tokens, app shell, sidebar-routed IA | (Part A) Design tokens: six Tailwind v4 `@theme` tokens (`--color-bg` #04100b, `--color-surface` #0b1a12, `--color-sidebar` #061610, `--color-sidebar-active` #122720, `--color-accent` #44e0a4, `--color-accent-soft` #54e7b3) in `frontend/src/index.css`; dark-only app (`class="dark"` on `<html>`, no light mode/toggle). RISK_COLORS unchanged. (Part B) Typography: `--font-sans` full system-font stack (Geist Variable + platform sans); `--font-mono` = `ui-monospace, JetBrains Mono, IBM Plex Mono, Cascadia Code, Fira Code, Menlo, Monaco, Consolas, monospace`. (Part C) App shell: `AppShell.tsx` persistent left sidebar (`bg-sidebar`) with nav (Scan Runs + New Scan always; Overview/Artefacts/Recommendations/Exports shown only when scan open, each to `/app/scans/:id/<section>`); top bar with scan target + status badge + Run new scan button. `ScanProvider` context wraps `/app/*`, polls scan by route param. (Part D) Routing: `/app/*` = AppShell; `/app` index = ScanListPage; `/app/scans/:id` redirects to `.../overview`; four new route pages under `pages/scan/` (ScanOverviewPage, ScanArtefactsPage, ScanRecommendationsPage, ScanExportsPage). All links updated. `npm run build` clean. | Done | — | 2026-09-09 |
| 25: Landing page (marketing/intro at root route) | New `LandingPage.tsx` at root `/`, scan list moved to `/app/scans`. 9 sections: (1) fixed nav bar with ECDAT wordmark (Lock icon + text), in-page anchor links (Threat / How it works / Platform), single "Open Dashboard" CTA — no sign-in. (2) Hero: bold headline with `--color-accent-soft` highlighted keyword, subhead explaining what ECDAT does, two CTAs (primary + secondary anchor), feature-chip row (CycloneDX 1.6 CBOM · Mosca's inequality · NIST FIPS 203/204/205 · classically broken vs quantum vulnerable), static console card with pulsing "live" dot and realistic demo_repo numbers (4 files / 13 detections / 6 critical / 7 quantum-safe). (3) Threat: 4 stat cards (NIST CNSA 2.0 2030 key-establishment deadline, 2031 digital-signature deadline, harvest now/decrypt later, 10–15 yr shelf life) with count-up animation on numeric stats. (4) How it works: 3 numbered pipeline cards (01 Discover, 02 Assess, 03 Recommend) mapped to real scanner pipeline. (5) Platform: 6 feature grid cards — all verified real capabilities (Full CBOM, classically broken vs quantum-vulnerable differentiator, local-first self-hosted, audit-ready exports, per-artefact overrides, Mosca's inequality scoring). (6) Inside the console teaser: simplified mock console + 4 bullet capabilities + CTA. (7) No pricing section. (8) FAQ: 6-item accordion (shadcn Accordion) with real Q&As. (9) Final CTA band. (10) Footer: ECDAT tagline, Product/Resources link groups, SIH 2026 disclaimer. Scroll-reveal animations via IntersectionObserver + CSS transitions (fade+rise), stat count-up on first view. Routing: `/` → LandingPage, `/app` index redirects to `/app/scans`, `/app/scans` → ScanListPage. AppShell sidebar + TopBar nav updated to `/app/scans`. `npm run build` (`tsc -b && vite build`) clean, zero TS errors. Every claimed capability verified against AGENTS.md feature list. | Done | — | 2026-09-09 |
| 26: Overview reskin, algorithm-mix + exposure-by-family charts, actionable priority cards | (Part A) Shared `useCountUp` hook extracted from LandingPage into `frontend/src/lib/useCountUp.ts` for reuse; LandingPage updated to import from shared module (verified build clean). (Part B) `ScanOverviewPage.tsx` full rewrite: stat cards restyled with `bg-surface` background + hover-lift micro-interaction (`hover:-translate-y-0.5 hover:shadow-lg hover:shadow-accent/5` CSS transition on all cards including MetaCard, chart cards, and priority cards); count-up animation on all numeric stat card values (total detections, files scanned, quantum vulnerable, classically broken) using shared `useCountUp` hook; vulnerable share keeps formatted percentage as-is. (Part C) **Algorithm mix donut chart** (Recharts `PieChart` + `Pie` with `innerRadius={55}`) driven by `summary.algorithm_family_counts` (field name verified against `ecdat_core/cbom_export.py:154` — the dict from `Counter(det.algorithm_family)`); 12-hue `FAMILY_PALETTE` for distinct family coloring; sorted by count descending. (Part D) **Exposure by algorithm family stacked bar chart** — per-family-per-risk-level counts computed client-side from the artefacts list (`page_size=500`, same approach Phase 18 used for classically-broken count) rather than adding a new backend endpoint. `computeFamilyRiskData()` groups artefacts by `algorithm_family` × `risk_assessment.risk_level`, produces flat rows keyed by risk level for Recharts `BarChart` stacking. Sorted by critical count descending so the most-at-risk families surface first. `useMemo` ensures recomputation only when artefacts change. (Part E) **Actionable top-5 priority cards** — refactored from the old numbered list into recommendation-oriented cards: each card phrases the action as "Replace {algorithm_family} in {file_path} with {recommended_algorithm}" using the artefact's own `recommendation.recommended_algorithm` (looked up by `detection_id` from the already-fetched artefacts map), with the existing `RiskLevelBadge` and a "View →" link navigating to `/app/scans/:id/artefacts?highlight={detection_id}`. (Part F) Skipped per task instructions: migration progress percent-complete widget (no backing data model), risk-vs-remediation trend line (no repeated scans of the same target in the database — the demo_repo fixture is re-scanned per test run on a fresh throwaway SQLite, so no real historical data exists to plot). `npm run build` (`tsc -b && vite build`) clean, zero TS errors. | Done | — | 2026-09-09 |
| 27: Artefacts/Recommendations/Exports reskin, consistent design system pass | (Part A) Shared `.stagger-enter` CSS animation utility in `frontend/src/index.css` (`@keyframes stagger-enter`: opacity 0→1 + translateY 4px→0, 200ms ease-out forwards; `animation-delay: calc(var(--stagger-index, 0) * 80ms)` capped at 8 so full pages don't drag; `prefers-reduced-motion: reduce` respected) applied to artefact table rows, recommendation family cards, and top-5 priority cards — deliberately quieter than the landing page's scroll-reveal. (Part B) `ArtefactsTab.tsx`: filter controls wrapped in a `border-border bg-bg/30` panel, Search input added (client-side placeholder), table rows use `stagger-enter` with `--stagger-index` per-row (cast `as CSSProperties` so TS accepts the custom property), SkeletonRows restyled, pagination buttons `border-border hover:bg-accent/5 hover:text-accent`. (Part C) `ArtefactDetailDialog.tsx`: dialog surface `bg-surface border-border`, header with X close button, sections `border-border bg-bg/30`, matched-text `<pre>` `bg-bg/50`, override-form inputs `border-border bg-background` (`bg-bg/50` read-only fields), Save button `bg-accent text-accent-foreground hover:bg-accent-soft`. (Part D) `MoscaTimeline.tsx` bar border `border`→`border-border`; `badges.tsx` verified already consistent (no changes). (Part E) `ScanRecommendationsPage.tsx`: families sorted critical→quantum-safe, cards `bg-surface` + hover-lift + `stagger-enter`, explicit `text-foreground` colors so nothing inherits a light-theme default; `ScanExportsPage.tsx`: cards `bg-surface` + hover effects, export buttons `border-border hover:bg-accent/10 hover:text-accent`, `FileJson`/`FileText` icons, `flex-wrap` on button row. (Part F) `ScanDetailPage.tsx` legacy tabs: StatCard + risk-distribution/top-5/MetaCard/empty/family cards all `bg-surface` + hover-lift, top-5 items `bg-bg/40 border-border` with recommendation-aware "Replace X with Y" layout; fixed latent structural JSX bug (file_path span nested in the wrong div), added missing `useMemo` import + `artefactMap` map, `truncatePath` import from `@/lib/paths`. Verified `AppShell.tsx` already uses `bg-sidebar-active`/`text-accent`/`STATUS_VARIANT` correctly — no changes needed there. No backend / `ecdat_core` / `signatures.json` changes. Verification: `npm run build` (`tsc -b && vite build`) zero TS errors (CSS custom properties on `style` cast via imported `CSSProperties` type); `npm run lint` only pre-existing shadcn fast-refresh + set-state-in-effect warnings; full pytest suite 142 passed (2 network deselected) | Done | — | 2026-09-10 |
| 28: Responsive pass, design-system cross-check, redesign verification | Part A phone-width quick pass: AppShell sidebar collapses to a 56px icon rail below `md` (labels hidden, `title`/`aria-label` retained, brand becomes a `Link` back to the landing page at `/`), New Scan form grid wraps to `grid-cols-1` on phones, Overview charts render via `ResponsiveContainer` instead of fixed widths, ArtefactDetailDialog action row wraps, top-5 priority cards gained `min-w-0` + `[overflow-wrap:anywhere]` so long mono file paths wrap instead of clipping. Part B cross-check passed: zero `fetch`/`axios` outside `src/api/client.ts`, and every hardcoded color traces to the Phase 24 token set or the documented `RISK_COLORS`/`FAMILY_PALETTE` palettes (one `#0b1a12` Tooltip background is a deliberate duplicate of `--color-surface` — Recharts inline styles can't resolve CSS custom properties). Part C clean-stack verification: `docker compose down -v` → `up --build` (4/4 healthy), then a full real-browser walkthrough in headless Chromium via CDP — landing at 1280/375/320px (zero horizontal overflow, all sections render) → Open Dashboard → created a demo_repo scan from the UI (13 detections) → Overview with all three new charts populated (6 critical / 7 quantum-safe / 7.7% vulnerable) → Artefacts (risk filter → exactly 6 critical rows; override PATCH on hasher.py:10 persisted, migration-time 0.5→2.0 re-ran `assess_risk`) → Recommendations (all five recommendation types correct, incl. ML-KEM "no migration needed") → Exports (CBOM downloads as valid CycloneDX 1.6: 13 `cryptographic-asset` components + sanctioned `ecdat:` properties; report returns the `export_summary` shape) → back to landing via the sidebar brand link. ScanDetailPage.tsx left unreachable-but-present (dead code since Phase 24 routing; deletion out of scope for a quick pass). No backend or `ecdat_core` changes. | Done | — | 2026-09-10 |
| 30: Render Blueprint, dynamic PORT binding, git in container image | **Part A — port binding**: `backend/Dockerfile` `CMD` switched from exec form to JSON-array `["sh", "-c", ...]` (variable expansion + no lint warning): `exec uvicorn app.main:app --app-dir /app/backend --host 0.0.0.0 --port "${PORT:-8000}"`. Exec form would pass `${PORT:-8000}` literally (no expansion); plain shell form draws a `JSONArgsRecommended` lint warning and leaves pid1 as `sh` (dash doesn't forward SIGTERM) — the `exec` makes uvicorn pid1 so Render's SIGTERM hits the right process. Locally Docker Compose leaves `$PORT` unset (its `environment:` block doesn't pass it), so the default 8000 matches the `8000:8000` mapping; the compose dev command (exec-form `--reload --port 8000`) is unchanged. **TWO LATENT RENDER-PATH BUGS FOUND AND FIXED by a probe that ran the image's real entrypoint + default CMD (no compose overrides) against the compose Postgres/Redis**: (1) `app.main` was not importable from the Dockerfile's `WORKDIR /app` (compose only worked because `working_dir: /app/backend` puts the app package on sys.path) → added `--app-dir /app/backend` to the default CMD; (2) the entrypoint's `alembic upgrade head` failed from `/app` (no `alembic.ini` → "No 'script_location' key found", exit 255 before uvicorn) → entrypoint now `cd /app/backend` first (a no-op under compose). **Part B — git in image**: new `apt-get` step (`apt-get update && apt-get install -y --no-install-recommends git && rm -rf /var/lib/apt/lists/*`) — there was NO existing apt step to add to; `python:3.12-slim` ships no git. Locally verified: `docker compose exec api git --version` → `git version 2.47.3`, and `git ls-remote https://github.com/...` works from inside the api container (outbound git path for `ingest_git_url` exercised — AGENTS.md's "unverified in a real container" concern now cleared locally; Render deploy still first real test). **Part C — health check**: `GET /health` (`backend/app/main.py`) returns a pure `{"status": "ok"}` dict — zero Postgres/Redis I/O in the handler (SQLAlchemy engine creation is lazy). Deliberate choice: keep it as a minimal liveness check, no DB probe added — Render's health checks hit it repeatedly and a DB-dependent check would cause spurious restarts when Postgres is briefly unreachable. **Part D — render.yaml Blueprint**: full free-tier topology (ecdat-api = Docker web service with `plan: free` — must be explicit: per Render docs an omitted plan defaults a web service to the *paid* `0.5c-512mb`; ecdat-web = static site with `./frontend/dist` + SPA `/* → /index.html` rewrite; ecdat-db = free Postgres via `databases:`; ecdat-redis = free Key Value via `services:` with `type: keyvalue` + `ipAllowList: []` internal-only). **Documented deviation from the draft blueprint**: the draft's root-level `keyvalue:` block is NOT in Render's documented root-level field list (`services`/`databases`/`envVarGroups`/`projects`/`ungrouped`/`previews`) — current docs place Key Value instances in `services` with `type: keyvalue`; restructured accordingly, keeping the `fromService: { type: keyvalue, name: ecdat-redis, property: connectionString }` cross-reference identical. Validated programmatically against the official JSON Schema (`https://render.com/schema/render.yaml.json` via jsonschema 4.26.0 + pyyaml): **0 errors**. `CORS_ORIGINS` and `VITE_API_BASE_URL` left `sync: false` deliberately — chicken-and-egg: each service needs the other's URL, which only exists after the first Apply (see Phase 31 note below this table). **Part E**: full pytest suite passes unchanged (142 passing, 2 network deselected); `docker compose up --build` → 4/4 healthy; api answers on 8000 and web proxy on 5173; Render-equivalent probe (real entrypoint + default CMD, `PORT=8123`) boots uvicorn on 8123 and answers `/health`. **Environment note**: an unrelated machine-level break (daemon.json `"dns": ["172.17.0.1"]` with no resolver there — Chromium had grabbed the port) blocked container DNS during this phase; fixed by removing the `"dns"` override from `/etc/docker/daemon.json` + daemon restart (modern Docker auto-detects systemd-resolved uplinks). No `ecdat_core`/backend logic changed. | Done | 8ad5a2a | 2026-09-10 |
| 31: Render deployment live, cross-service env vars wired, verified end to end | **Fix Phase 6 — crash-loop fix**: first deploy crash-looped with `ModuleNotFoundError: No module named 'psycopg2'`. SQLAlchemy's `postgresql://` dialect defaults to psycopg2, but only psycopg3 (`psycopg[binary]`) was installed. Added `psycopg2-binary` to `backend/pyproject.toml` dependencies. Container came up on next deploy. **Part A — cross-service env vars**: `CORS_ORIGINS=https://ecdat-web.onrender.com` set on ecdat-api, `VITE_API_BASE_URL=https://ecdat-api.onrender.com` set on ecdat-web. Both services redeployed (static site rebuilt to bake in build-time env var). Verified: CORS preflight returns `access-control-allow-origin: https://ecdat-web.onrender.com`; `ecdat-api.onrender.com` found in built JS bundle. **Part B — live verification** (all against real Render URLs, not localhost): (1) `GET /health` → `{"status":"ok"}`; (2) `GET /api/scans` → `[]` empty list; (3) `POST /api/scans` local_path against `/app/ecdat_core/tests/fixtures/demo_repo` (container-internal fixture path, confirmed working) → 4 files, 13 detections, 6 critical, 7 quantum-safe, 1 quantum-vulnerable (7.69%) — matches ground truth exactly; (4) `POST /api/scans` git_url against `https://github.com/profm0r14rty/ECDAT` → 49 files, 196 detections across 11 algorithm families (AES/MD5/RSA/DES/ML-KEM/ML-DSA/SLH-DSA/3DES/RC4/SHA-1/ECDSA), 86 critical, 52 quantum-safe, 58 low — exercises the Phase 30 git-install fix (first real `git clone` from inside a Render container); (5) `GET /api/scans/{id}/cbom` → valid CycloneDX 1.6, 13 components. **Part C — infra**: Postgres and Redis both provisioned and reachable (proven by scan creation + persistence + background job execution). Free tier capacity adequate — no provisioning failures. Live URLs: API `https://ecdat-api.onrender.com`, Dashboard `https://ecdat-web.onrender.com`. Full test suite: 142 passing (2 network deselected). | Done | abf0b12 | 2026-09-10 |
| 32: Live deployment documented, warm-keeping guidance, Batch 4 complete | **Part A — Live Deployment**: Added a "Live Deployment" section to the root `README.md` with the real Render API and Dashboard URLs. Included a note explaining the free-tier cold-start behavior (15 min sleep, ~1 min wake) and advising to warm the API before demos. **Part B — Warm-keeping guidance**: Appended a "Keeping the API warm" note to `AGENTS.md`'s Render deployment section, mentioning UptimeRobot's free tier as a set-and-forget alternative to manually opening the URL. **Part C — Batch 4 Summary**: Added a summary paragraph to `progress.md` documenting the deployment platform, what's live, the git availability fix required for the container, and verification performed. **Phase 30 — git in image**: fixed the missing `git` binary in `python:3.12-slim` (added `apt-get install -y git` to Dockerfile), enabling `git_url` scans against remote repos from within the Render container. Verified against `https://github.com/profm0r14rty/ECDAT` during the live Phase 31 verification. Full test suite: 142 passing. | Done | — | 2026-09-10 |
| 33: Showcase demo data with realistic multi-family risk spread | New `ecdat_core/tests/fixtures/showcase_repo/` fixture: 8 source files across Python, JavaScript, Java, and Go spanning 12 algorithm families (RSA, ECDSA, ECDH, ECC, MD5, SHA-1, DES/3DES, AES, DH, ML-KEM, ML-DSA, SLH-DSA) — deliberately designed for a rich judge demo with a wide risk spread: 16 critical (RSA-1024 auth signing, MD5 password hashing, SHA-1 certificate fingerprinting, 3DES encryption), 8 medium (AES-128 metrics, DH 2048-bit key exchange), 8 low (ECDSA/ECDH/ECC signing and key generation), 38 quantum-safe (AES-256-GCM payment encryption, ML-KEM-768 key encapsulation, ML-DSA-65/SLH-DSA-128f post-quantum signatures). Pushed to `https://github.com/profm0r14rty/ecdat-showcase` as a public repo for live `git_url` scanning. Verified against the live Render API: scan `5b4033fd` completed in ~3s via `git_url`, 8 files / 70 detections / 12 families — identical counts to local CLI scan. All four dashboard data endpoints confirmed: artefact listing with risk-level filtering (critical: 4 families, medium: 3, low: 3, quantum-safe: 3), valid CycloneDX 1.6 CBOM with 70 components, and summary report. Local Docker Compose scan `5d010fee` also confirmed identical numbers. No existing test assertions touched — fixture is presentation material, not wired into pytest counts. | Done | — | 2026-09-10 |
| 34: DEMO.md walkthrough script, live QA pass | **Part A — DEMO.md**: New `DEMO.md` at repo root with a 3-minute judge demo script: one-sentence hook (CNSA 2.0 2030/2031 deadlines), exact click path through the showcase scan (scan `5b4033fd`, 8 files / 70 detections / 16 critical / 38 quantum-safe), the classically-broken vs quantum-vulnerable differentiator moment (MD5 in `login.py:75` = classically broken / critical, RSA-1024 in `login.py:24` = quantum-vulnerable / critical, AES-256 in `payment.py:4` = quantum-safe), CBOM download as closing beat (CycloneDX 1.6, 70 `cryptographic-asset` components), and four anticipated Q&As. **Part B — Live QA pass** against `https://ecdat-api.onrender.com` and `https://ecdat-web.onrender.com`: all API-level numbers verified match DEMO.md exactly (70 detections, 16 critical, 8 medium, 8 low, 38 quantum-safe, 17 quantum-vulnerable @ 24.3%); three key demo artefacts verified (MD5 classically_broken/critical/urgency 10.5, RSA quantum_vulnerable/critical/urgency 2.33, AES-256 quantum-safe); CBOM validated (bomFormat=CycloneDX, specVersion=1.6, 70 cryptographic-asset components, `ecdat:` properties); CORS preflight correct (`access-control-allow-origin: https://ecdat-web.onrender.com`); frontend bundle has `ecdat-api.onrender.com` baked in; API health returns `{"status":"ok"}` in 0.47s; landing page JS contains all claimed content (2030, 2031, CycloneDX, Mosca, FIPS 203, ML-KEM, classically broken). Playwright browser QA blocked by missing Chrome in env — API-level verification covers all critical claims. | Done | — | 2026-09-10 |
| 35: Fix stuck backend-unreachable badge (self-healing reachability state) | **Root cause confirmed**: `ScanListPage.tsx` health check used `useEffect(() => {...}, [])` — ran once on mount, set `'unreachable'` on error, never re-checked. No periodic poll, no clearing mechanism. Once the badge hit "unreachable" (e.g. during Render cold-start timeout), it stayed stuck for the entire page session even if subsequent `listScans()` succeeded. The scan list's error state had the same one-shot bug. **Fix (self-healing reachability)**: (1) `HealthState` extended from `'checking' | 'ok' | 'unreachable'` to `'checking' | 'waking-up' | 'ok' | 'unreachable'` — the new `'waking-up'` state distinguishes Render's expected ~60s cold-start (neutral badge, no panic) from a genuine outage (red "Backend unreachable"). (2) Health probe now runs on a 30s `setInterval` (`HEALTH_POLL_INTERVAL_MS`), not just once on mount; cleanup clears the interval on unmount. (3) Cold-start grace period: first failed probe transitions to `'waking-up'` and starts a 90s timer (`WAKE_UP_GRACE_MS`); if the timer expires without a successful probe, the state advances to `'unreachable'`. Any successful probe cancels the timer and resets to `'ok'`. (4) `ScanList` accepts an `onBackendReachable` callback; on successful `listScans()`, it calls the callback, which also cancels the wake-up timer and sets health to `'ok'` — so the badge heals from data-fetch success even if the health endpoint is temporarily unreachable. (5) Badge rendering: `'waking-up'` uses `variant="outline"` (neutral) with text "Backend waking up…"; `'ok'` uses `variant="default"` (green); `'unreachable'` uses `variant="destructive"` (red). **Cold-start UX decision (Part C)**: Implemented inline — the `'waking-up'` state is a natural extension of the fix, not a separate Phase 38 concern, because: (a) the state machine already needed three states for the grace-period logic, (b) adding the distinct badge text is ~10 lines of JSX, and (c) deferring it would leave the grace period logic invisible to users (they'd see "Backend unreachable" for 90s during a normal cold start, which defeats the purpose). **Browser verification (Part D)**: Chromium headless shell could not launch in this sandboxed environment (binary hangs on pipe setup, likely missing compositor dependencies). curl confirmed live endpoints reachable (API 200 in 2.8s, frontend 200 in 3.0s). Manual verification steps: open `https://ecdat-web.onrender.com/app/scans`, watch badge transition from "Checking backend…" → "Backend ok" (or "Backend waking up…" if cold), wait 30s and confirm badge re-checks, kill backend / wait for timeout and confirm "Backend unreachable" appears. Full test suite: 142 passing. `tsc -b` clean. | Done | — | 2026-09-10 |

**Phase 31 prerequisite — first-deploy chicken-and-egg (do NOT skip):** the two `sync: false` env vars in `render.yaml` are intentionally unset on first Apply:
1. Apply the Blueprint → the four resources are created; `ecdat-api` gets a `*.onrender.com` URL and `ecdat-web` gets its own.
2. Set `CORS_ORIGINS` on `ecdat-api` to the `ecdat-web` URL (e.g. `https://ecdat-web.onrender.com`) — without it, the dashboard's browser requests to the API come from a different origin and are blocked by CORSMiddleware.
3. Set `VITE_API_BASE_URL` on `ecdat-web` to the `ecdat-api` URL (e.g. `https://ecdat-api.onrender.com`) — this triggers a static-site rebuild, so the built bundle knows which API to call.
4. Redeploy/verify: `curl https://<ecdat-api>.onrender.com/health` → `{"status":"ok"}`; dashboard loads and the health badge reports "Backend ok".
Also: warm the API before demos (free tier sleeps after 15 min of inactivity, ~1 min cold start) — see AGENTS.md "Cold starts" note and the "Keeping the API warm" section for automated/manual options.

### CLI Summary (corrected output)

```
$ python -m ecdat_core.cli scan /home/pratyay/Documents/My Codes/ECDAT/ecdat_core/tests/fixtures/demo_repo

wrote cbom.json
wrote summary.json

Scan target : /home/pratyay/Documents/My Codes/ECDAT/ecdat_core/tests/fixtures/demo_repo
Files scanned: 4
Detections  : 13
--------------------------------
Risk level       Count
--------------------------------
critical             6
high                 0
medium               0
low                  0
quantum-safe         7
--------------------------------
```

*Note: totals corrected from the earlier 17/7/10 by Fix Phase 5's same-line
dedup (one Detection per signature+line; the AES bare-identifier and the MD5
generic-`md5(` duplicates each collapsed into their more-specific sibling).*

### Phase 8 — CLI run against `fixtures/demo_repo`

```
$ python -m ecdat_core.cli scan ecdat_core/tests/fixtures/demo_repo

wrote cbom.json
wrote summary.json

Scan target : .../ecdat_core/tests/fixtures/demo_repo
Files scanned: 4
Detections  : 13
--------------------------------
Risk level       Count
--------------------------------
critical             6
high                 0
medium               0
low                  0
quantum-safe         7
--------------------------------
```

*Note: re-recorded with Fix Phase 5's corrected totals (was 17/7/10 before
same-line dedup).*

## Batch 2 Summary

ECDAT's FastAPI backend (Batch 2) is complete and tested end-to-end. It now
exposes the entire scanner pipeline as a REST API: `POST /api/scans` creates a
queued scan run and executes it in the background (FastAPI `BackgroundTasks` —
no Celery, keeping infra lean for the hackathon deadline), with Redis holding
the `scan:{id}:status` job keys and Postgres persisting the full ScanResult
via SQLAlchemy 2.0 ORM rows managed by a repository layer and Alembic
migrations. The API never duplicates `ecdat_core` logic — it calls
`ecdat_core.cli.run_scan` / the risk engine / CBOM exporter directly and
persists the result. Read paths reconstruct the ScanResult from persisted rows
on every request, so per-artefact risk overrides applied via
`PATCH /api/scans/{id}/artefacts/{detection_id}` are reflected everywhere,
including the CycloneDX 1.6 CBOM download. Docker Compose wires the whole
stack (postgres + redis + api, 3/3 healthy, entrypoint auto-migrates). A
Phase 15 end-to-end integration test drives the full Batch 2 API surface in a
single flow against a throwaway SQLite database with Redis faked: scan
creation → status polling → artefacts listing with the Fix Batch's corrected
counts and the classically-broken/Mosca-critical distinction → CBOM
structural validation → risk-override persistence. The full suite stands at
139 passing tests (2 network-gated tests deselected by default).

## Batch 3 Summary

ECDAT's interactive dashboard (Batch 3) is complete and demoable end to end. The `frontend/` React + Vite + TS app talks to the Batch 2 API exclusively through a single typed client (`src/api/client.ts`, whose interfaces mirror the Pydantic response models 1:1) and covers the full workflow in the browser: create a scan from a Git URL or local path → watch it poll to completion with live status → land on a scan detail page whose Overview tab shows stat cards (total detections, files scanned, quantum-vulnerable count + share, classically broken count), a risk-distribution pie chart and a top-5 highest-urgency list; the Artefacts tab is a filterable/paginated table (risk level, algorithm family, page size) whose row click opens a detail dialog with the matched source text, the Mosca X/Y/Z timeline (distinct visual states for classically-broken and genuinely quantum-safe artefacts, keeping the two risk concepts separate per the Fix-pass bug), and a risk-override form that re-runs `assess_risk` and persists via PATCH; the Recommendations tab groups artefacts by algorithm family into an executive-style summary of affected counts and NIST PQC replacements; the Export tab downloads the real CycloneDX 1.6 CBOM and the summary report. Docker Compose now runs the whole four-service stack — postgres + redis + api + web — with the `web` container serving the production build from nginx and reverse-proxying `/api` + `/health` to the API, so the bundle stays same-origin with zero runtime env substitution. Phase 22 added the final polish: route-driven page titles, a top-level error boundary, skeleton loading states, and WCAG-AA-verified risk palette colors, all verified in a clean-state containerized run in a real browser. The full suite stands at 142 passing tests (2 network-gated tests deselected by default); Batch 3 added no backend or `ecdat_core` changes.

## Frontend Redesign Summary

ECDAT's frontend redesign (Phases 24–28) is complete and verified end to end in a real browser against a clean-state containerized run. The dashboard moved from a utilitarian table view to a dark, mint-accented design system (six `@theme` tokens `#04100b`–`#54e7b3` in `frontend/src/index.css`, dark-only, system font stacks) with a sidebar-routed information architecture under `/app/*`: a persistent AppShell with scan-context-aware nav (Scan Runs + New Scan always; Overview/Artefacts/Recommendations/Exports only when a scan is open), a marketing landing page at the root route (threat stats with count-up animation, How-it-works pipeline, Platform capability grid, FAQ accordion, final CTA — every claim a real capability, no pricing), and a fully reskinned scan detail flow split into four tabs: Overview (stat cards with count-up + hover-lift, risk-distribution pie, algorithm-mix donut, exposure-by-family stacked bar, recommendation-oriented top-5 priority cards), Artefacts (filter panel, stagger-animated rows, detail dialog with Mosca timeline), Recommendations (executive per-family cards with affected counts + NIST PQC replacements), and Exports (CBOM + report downloads). Phase 28 closed the loop with the responsive quick pass: the sidebar collapses to a 56px icon rail below `md`, the New Scan form wraps to a single column on phones, all overview charts render through `ResponsiveContainer`, and long file paths in priority cards wrap instead of clipping. A design-system cross-check confirmed no direct `fetch`/`axios` was introduced outside `src/api/client.ts` and every hardcoded color belongs to the sanctioned token/palette set. The full browser walkthrough on a fresh `docker compose down -v && up --build` (4/4 healthy): landing (verified at 1280/375/320px, zero horizontal overflow) → Open Dashboard → demo_repo scan created from the UI and polled to completion (13 detections) → Overview with all three charts populated and the corrected Fix-Batch counts → Artefacts (risk filter → 6 critical rows; override PATCH persisted, migration-time 0.5→2.0 on hasher.py:10 re-ran `assess_risk`) → Recommendations (all five recommendation types correct, including the ML-KEM "already using PQC, no migration needed" case) → Exports (CBOM downloads as valid CycloneDX 1.6 with 13 `cryptographic-asset` components and the sanctioned `ecdat:` property set; report returns the `export_summary` shape) → back to the landing page via the sidebar brand link. The full suite stands at 142 passing tests (2 network-gated tests deselected by default); the redesign added no backend or `ecdat_core` changes.

## Batch 5 Summary

ECDAT is now live on Render's free tier and demo-ready. The showcase scan (scan `5b4033fd`, cloned from `https://github.com/profm0r14rty/ecdat-showcase` via `git_url`) demonstrates the full capability surface: 8 files across Python, JavaScript, Java, and Go with 70 detections spanning 12 algorithm families, a wide risk spread (16 critical, 8 medium, 8 low, 38 quantum-safe), and a valid CycloneDX 1.6 CBOM. The DEMO.md walkthrough script captures the 3-minute judge demo flow: warm the API, open the showcase scan, point at the risk-distribution pie chart (critical = classically broken, not quantum-vulnerable), drill into the MD5 password-hashing artefact (classically broken = true, quantum vulnerable = false — the key differentiator most naive scanners miss), contrast with RSA-1024 (quantum vulnerable = true, classically broken = false), show AES-256 as quantum-safe, and close with the CBOM download proving it's the real CycloneDX standard. All cross-service wiring verified: `CORS_ORIGINS` on the API allows the dashboard origin, `VITE_API_BASE_URL` baked into the static site bundle points at the API, and the `git_url` scan path works because the container image now includes `git` (Phase 30 fix). Full test suite: 142 passing (2 network-gated deselected).
